class Product:
    def __init__(self, name, price):
        self.name = name
        self.price = price

class ShoppingCart:
    def __init__(self):
        self.products = []
        self.total_amount = 0
        self.discount_amount = 0

    def add_product(self, product, quantity, is_gift_wrapped):
        product_total = product.price * quantity
        gift_wrap_fee = 1 * quantity if is_gift_wrapped else 0
        self.total_amount += product_total + gift_wrap_fee
        self.discount_amount += self.calculate_discounted_price(product.price, quantity)

        print(f"{product.name} - Quantity: {quantity}, Total Amount: {product_total}")

    def calculate_discounted_price(self, product_price, quantity):
        # ... (same discount calculation logic)

    def calculate_shipping_fee(self):
        return 5 * ((self.total_amount // 10) + (1 if self.total_amount % 10 != 0 else 0))

    def display_summary(self):
        shipping_fee = self.calculate_shipping_fee()

        print("\nSummary:")
        print(f"Subtotal: {self.total_amount}")
        print(f"Discount Applied: {self.discount_amount}")
        print(f"Shipping Fee: {shipping_fee}")
        print(f"Total: {self.total_amount - self.discount_amount + shipping_fee}")

def main():
    product_a = Product("Product A", 20)
    product_b = Product("Product B", 40)
    product_c = Product("Product C", 50)

    cart = ShoppingCart()

    # Assuming user input for quantity and gift wrapping
    cart.add_product(product_a, 5, True)
    cart.add_product(product_b, 15, False)
    cart.add_product(product_c, 10, True)

    cart.display_summary()

if __name__ == "__main__":
    main()
